#include<stdio.h>

int main(){
    int num;
    printf("Number: ");
    scanf("%d", &num);
    
    if (num < 1)
    {
        printf("not a natural");
    }
    else(
        printf("yes it is")
    )
    
}