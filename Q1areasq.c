#include <stdio.h>

// area of square

int main()
{
    int sides;
    printf("what is the size of the square of each : \n");
    scanf("%d", &sides);

    printf("area of the square is : %d", sides*sides);
    return 0;
}