#include <stdio.h>

int main()
{
    // for numbers if integers then int and if decimals then double/floats
    // for number you dont need to put semicolons
    int age = 29;
    double gpa = 2.44;

    // for names or character use char
    // in char if more than one variable is present then use [] and semicolons is necessary
    char name = 'fahad'; 
    char names[] = "fahad" "afifa";
}   